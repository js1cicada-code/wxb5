# 底部确认栏样式优化

## 修改说明

参考500彩票网(https://trade.m.500.com/lottery_new_v2/jczq/order/nspf)的设计，优化了底部确认栏的样式和交互。

## 主要改进

### 1. 三列布局
```
┌─────────────────────────────────────┐
│  清空  │  已选X场  │  确认选号   │
│        │  共X注    │             │
└─────────────────────────────────────┘
```

### 2. 样式特点

| 特点 | 说明 |
|------|------|
| 固定底部 | position: fixed; bottom: 0 |
| 阴影效果 | box-shadow: 0 -1px 6px rgba(0,0,0,0.1) |
| 主色调 | 红色 #f63f3f |
| 按钮样式 | 圆角4px，高度约40px |
| 清空按钮 | 带图标的文字按钮 |

### 3. 过关方式区域

- 展开时显示过关方式选择
- 投注倍数控制
- 预计奖金显示

## CSS样式

```css
/* 底部栏 */
.bottom-bar {
    position: fixed;
    bottom: 0;
    left: 0;
    right: 0;
    background: #fff;
    z-index: 100;
    box-shadow: 0 -1px 6px rgba(0,0,0,0.1);
}

/* 主色调 */
--primary-red: #f63f3f;
--primary-red-dark: #e02e2e;
```

## 对比

### 修改前
- 弹性布局，元素挤在一起
- 清空按钮是红色边框
- 过关方式直接显示
- 整体比较紧凑

### 修改后
- 三列清晰分隔
- 清空按钮带图标，更优雅
- 过关方式区域独立展开
- 更像专业彩票App

## 文件修改

1. **index.html**
   - CSS样式：约25行新增/修改
   - HTML结构：底部栏重新布局

## 测试

```bash
# 启动服务
npm run dev

# 访问页面
http://localhost:8082
```

## 功能验证

- ✅ 三列布局正确显示
- ✅ 清空按钮功能正常
- ✅ 场次/注数显示正确
- ✅ 确认按钮状态切换
- ✅ 过关方式展开/收起
- ✅ 倍数控制功能正常
- ✅ 预计奖金计算正确

## 用户体验提升

1. **视觉清晰**: 左中右三列一目了然
2. **操作直观**: 清空按钮带图标更明显
3. **专业感**: 类似主流彩票App的交互
4. **信息突出**: 红色强调重要数字