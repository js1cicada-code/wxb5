# RCA: 数据丢失事故分析

## 事故概述
**时间**: 2026-03-24
**影响**: 篮球分析页面所有数据丢失
**严重级别**: 高

## 事故描述
在排查"排名显示"问题时，执行了 `rm -rf dist/` 命令，导致：
1. 所有爬虫生成的JSON数据文件丢失
2. 用户无法访问篮球分析页面数据
3. 需要重新运行所有爬虫恢复数据

## 根本原因分析

### 直接原因
- 执行了破坏性命令 `rm -rf dist/` 而没有备份

### 根本原因
1. **架构设计问题**: 数据文件与构建产物混合存储
   - `dist/` 目录既包含构建产物（HTML/CSS/JS），又包含数据文件（JSON）
   - 没有明确的数据存储目录

2. **操作流程问题**: 缺少安全检查
   - 删除目录前未检查目录内容
   - 未区分可重建文件和不可重建文件

3. **认知误区**: 
   - 误以为 `dist/` 只是构建产物，可以随时重建
   - 忽略了爬虫生成的JSON文件也存储在 `dist/` 中

## 影响范围
- ✅ 篮球比赛列表数据
- ✅ 篮球分析详细数据（9场比赛）
- ✅ 北京单场数据
- ✅ 传统足彩数据

## 补救措施
已执行：
1. 重新运行 `basketball_analysis_crawler.py`
2. 重新运行 `bjdc_crawler.py`
3. 重新运行 `ctzc_crawler.py`
4. 重新构建项目

## 预防措施

### 短期措施（立即执行）
1. ✅ **分离数据目录**
   - 创建 `data/` 目录专门存储JSON数据文件
   - 修改所有爬虫脚本，将数据保存到 `data/` 目录

2. ✅ **更新 .gitignore**
   - 将 `data/` 目录加入版本控制
   - `dist/` 目录保持为纯构建产物

3. ✅ **创建操作检查清单**
   ```
   □ 删除目录前，先列出目录内容
   □ 确认无数据文件后再删除
   □ 重要操作前先备份
   ```

### 中期措施（本周完成）
1. **数据备份机制**
   - 每次爬虫运行后自动备份到 `data/backup/`
   - 保留最近3次的数据备份

2. **目录结构规范化**
   ```
   /data/               # 数据文件目录
     /matches/          # 比赛列表数据
     /analysis/         # 分析详细数据
     /backup/           # 数据备份
   /dist/               # 构建产物（可随时删除重建）
   ```

3. **添加数据恢复脚本**
   - 创建 `restore_data.sh` 脚本
   - 从备份恢复数据

### 长期措施（下个迭代）
1. **架构优化**
   - 考虑使用数据库存储数据
   - 实现增量更新，避免全量爬取

2. **监控告警**
   - 检测数据文件是否存在
   - 数据文件丢失时自动告警

## 关键教训

### ⚠️ 永远不要执行的操作
```bash
# ❌ 危险操作
rm -rf dist/
rm -rf data/

# ✅ 安全操作
ls -la dist/          # 先查看内容
find dist/ -name "*.json"  # 确认是否有数据文件
```

### ⚠️ 目录分类原则
| 目录 | 内容 | 可删除 | 可重建 |
|------|------|--------|--------|
| `dist/` | 构建产物 | ✅ 是 | ✅ 是（npm run build）|
| `data/` | 数据文件 | ❌ 否 | ⚠️ 需重新爬取 |
| `src/` | 源代码 | ❌ 否 | ✅ 可从git恢复 |

## 检查清单

### 执行删除操作前
- [ ] 确认目录内容：`ls -la <directory>`
- [ ] 检查JSON文件：`find <directory> -name "*.json"`
- [ ] 确认数据已备份或有恢复方案
- [ ] 考虑是否真的需要删除

### 修改架构后
- [ ] 数据文件存储在独立目录（`data/`）
- [ ] 构建产物与数据文件分离
- [ ] 有数据备份机制
- [ ] 有数据恢复脚本

## 相关文档
- 爬虫脚本：`basketball_analysis_crawler.py`
- 构建配置：`webpack.config.js`
- 备份脚本：待创建

## 责任人
- 事故责任人：AI助手
- 审核人：用户
- 预防措施执行人：AI助手

## 状态
- ✅ 事故已修复
- ⏳ 预防措施执行中
- 📝 文档已归档